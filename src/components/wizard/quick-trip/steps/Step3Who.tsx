import { PlaceholderStep } from './_PlaceholderStep';

export function Step3Who() {
  return (
    <PlaceholderStep
      title="Step 3 — Who"
      subtitle="Inline tabs (Friends / Recent / Invite / Guest) refactored from AddPlayerSheet land in Phase 2.4."
    />
  );
}
