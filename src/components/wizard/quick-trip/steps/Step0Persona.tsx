import { PlaceholderStep } from './_PlaceholderStep';

export function Step0Persona() {
  return (
    <PlaceholderStep
      title="Step 0 — Persona Fork"
      subtitle="Three trip-type cards (Quick / Plan Ahead / Ryder Cup) + optional duplicate row land in Phase 2.1."
    />
  );
}
