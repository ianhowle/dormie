import { PlaceholderStep } from './_PlaceholderStep';

export function Step1Where() {
  return (
    <PlaceholderStep
      title="Step 1 — Where"
      subtitle="CourseLocationPicker integration + recent-courses smart default land in Phase 2.2."
    />
  );
}
