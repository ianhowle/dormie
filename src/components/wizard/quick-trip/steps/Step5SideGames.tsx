import { PlaceholderStep } from './_PlaceholderStep';

export function Step5SideGames() {
  return (
    <PlaceholderStep
      title="Step 5 — Side games"
      subtitle="Recent games + multi-select educated list with ⓘ disclosure + Skip option land in Phase 2.6."
    />
  );
}
