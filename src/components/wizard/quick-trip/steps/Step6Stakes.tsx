import { PlaceholderStep } from './_PlaceholderStep';

export function Step6Stakes() {
  return (
    <PlaceholderStep
      title="Step 6 — Stakes"
      subtitle="One PerGameStakeInput per selected format/game + Skip option land in Phase 2.7."
    />
  );
}
