import { PlaceholderStep } from './_PlaceholderStep';

export function Step2When() {
  return (
    <PlaceholderStep
      title="Step 2 — When"
      subtitle="Date picker + smart-defaults pill row (Today / Tomorrow / This Saturday) land in Phase 2.3."
    />
  );
}
